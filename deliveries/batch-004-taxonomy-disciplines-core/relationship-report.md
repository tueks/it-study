# Reporte de relaciones

Estado: `complete`. Se registraron 8 campos `homeDomain` y 64 referencias en `applicationDomains`, todos resueltos. Son campos de autoridad taxonómica, no relaciones ordinarias. Se crearon 0 objetos `relations[]`, 0 inversas persistidas y 0 terceros niveles.
