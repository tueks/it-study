# Reporte de excepciones

No se aplicaron excepciones.

El rebalanceo que incorpora `entity-algoritmo` y retira `entity-maquina-de-turing` de B006 no es excepción: forma parte de las asignaciones del plan maestro v2 aprobadas mediante `decision-0021`.
